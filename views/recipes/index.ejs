<%- include('../partials/header') %>

<h1>All Recipes</h1>

<table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Ingredients</th>
        <th>Steps</th>
        <th>Leave A Review</th>
      </tr>
    </thead>
    <tbody>
        <ul>
        <% recipes.forEach(function(r) { %>
            <li>
                <tr>
                    <td><%= r.title %></td>
                    <td><%= r.ingredients.join(', ') %></td>
                    <td><%= r.steps.join(', ') %></td>
                    <td><a href="/recipes/show">Reviews</a></td>
                </tr>
            </li>
        <% }); %>
        </ul>
    </tbody>
</table>
<a href="/recipes/new">Add Recipe</a>
  
<%- include('../partials/footer') %>
